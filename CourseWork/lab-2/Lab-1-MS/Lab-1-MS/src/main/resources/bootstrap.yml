spring:
  application:
    name: Lab-1-MS
  cloud:
    config:
      enabled: true
      uri: http://localhost:9898
      name: Lab-1-MS
      profile: test
    bus:
      enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"

